import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Piercing',
}

const page = (): React.JSX.Element => {
  return (
    <div>
        PIERCING
    </div>
  )
}

export default page