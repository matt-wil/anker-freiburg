import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Aktionen',
}

const page = (): React.JSX.Element => {
  return (
    <div>
        AKTIONEN
    </div>
  )
}

export default page