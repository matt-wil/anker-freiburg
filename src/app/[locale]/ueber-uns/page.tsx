import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Über uns',
}

const page = (): React.JSX.Element => {
  return (
    <div>
        ÜBER UNS
    </div>
  )
}

export default page