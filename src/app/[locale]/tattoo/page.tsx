import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Tattoo',
}

const page = (): React.JSX.Element => {
  return (
    <div>
        TATTOO
    </div>
  )
}

export default page