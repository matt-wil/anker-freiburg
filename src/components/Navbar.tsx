import Language from "@/components/Language"


const Navbar = (): React.ReactNode => {
  return (
    <header>
        <nav>

        </nav>
        <Language />
    </header>
  )
}

export default Navbar